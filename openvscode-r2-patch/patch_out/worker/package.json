{
  "name": "vscode-cloud-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "node --check src/index.js && node --check src/r2fs.js && node --check src/shellSandbox.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.4"
  }
}
